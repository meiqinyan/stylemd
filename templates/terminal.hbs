<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="generator" content="stylemd">
  <title>Terminal Output</title>
  <link href="https://fonts.googleapis.com/css2?family=VT323&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #000000;
      color: #00FF00; /* Green text */
      font-family: 'VT323', monospace; /* Retro terminal font */
      font-size: 16px;
      line-height: 1.2;
      padding: 15px;
      margin: 0;
    }
    .terminal-output {
        white-space: pre-wrap; /* Wrap long lines but preserve spaces */
        word-wrap: break-word; /* Break long words */
    }
    .terminal-prompt {
      color: #00FFFF; /* Cyan prompt */
    }
    .terminal-cursor {
      display: inline-block;
      background-color: #00FF00; /* Green cursor */
      width: 9px; /* Cursor width */
      height: 16px; /* Cursor height */
      animation: blink 1s step-end infinite;
      vertical-align: bottom;
    }
    @keyframes blink {
      50% { opacity: 0; }
    }
    /* Markdown content styling */
    h1, h2, h3 {
      color: #FFFF00; /* Yellow headings */
      margin-top: 1em;
      margin-bottom: 0.5em;
      font-weight: normal;
    }
    h1 { text-transform: uppercase; }
    ul, ol {
        padding-left: 0;
        margin: 0.5em 0;
        list-style: none;
    }
    li::before {
        content: "* "; /* Use asterisk for list items */
        color: #FF00FF; /* Magenta */
    }
    code {
      background-color: #222222;
      color: #FFFFFF; /* White code text */
      padding: 0.1em 0.3em;
    }
    pre {
      background-color: #111111;
      border: 1px solid #333333;
      /* Add inner shadow for depth */
      box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.5);
      padding: 10px;
      margin: 1em 0;
      overflow-x: auto;
      counter-reset: line; /* Reset line counter for each pre block */
      padding-left: 4em; /* Space for line numbers */
      position: relative;
    }
    pre code {
      background-color: transparent;
      border: none;
      padding: 0;
      display: block; /* Ensure code takes full width */
    }
    pre code::before {
      content: counter(line);
      counter-increment: line;
      position: absolute;
      left: 0;
      width: 3em; /* Width of line number area */
      padding-right: 1em;
      margin-left: -4em; /* Position numbers in the padding */
      text-align: right;
      color: #888888; /* Dim color for line numbers */
      /* Optional: prevent selection */
      -webkit-user-select: none;
      -moz-user-select: none;
      -ms-user-select: none;
      user-select: none;
    }
    blockquote {
      border-left: 3px solid #00FF00;
      padding-left: 10px;
      margin-left: 0;
      font-style: italic;
      color: #AAAAAA; /* Gray quote text */
    }
    a {
        color: #00FF00; /* Green links like main text */
        text-decoration: none; /* No underline */
    }
    a:hover {
        background-color: #00FF00;
        color: #000000;
    }
    table {
      border-collapse: collapse;
      margin: 1em 0;
      width: 100%;
      border: 1px solid #333333;
    }
    th, td {
      border: 1px solid #333333;
      padding: 0.3em 0.5em;
      text-align: left;
    }
    th {
      background-color: #222222;
      color: #FFFF00; /* Yellow header text */
      font-weight: normal;
    }
  </style>
</head>
<body>
  <div class="terminal-output">
    <span class="terminal-prompt">user@stylemd:~$</span> cat document.md
    {{{content}}}
    <span class="terminal-prompt">user@stylemd:~$</span> <span class="terminal-cursor"></span>
  </div>
</body>
</html> 