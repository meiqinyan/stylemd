<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Technical Blueprint</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@400;700&family=Architects+Daughter&display=swap" rel="stylesheet">
  <style>
    :root {
      --blueprint-bg: #00416a; /* Traditional Blueprint Blue */
      --blueprint-line: #ffffff;
      --blueprint-text: #e0f2f7; /* Light cyan text */
      --blueprint-accent: #a7ffeb; /* Teal accent */
      --blueprint-grid: rgba(255, 255, 255, 0.1);
      --blueprint-container-bg: rgba(0, 65, 106, 0.8);
    }
    body {
      background-color: var(--blueprint-bg);
      background-image: linear-gradient(var(--blueprint-grid) 1px, transparent 1px),
                        linear-gradient(90deg, var(--blueprint-grid) 1px, transparent 1px);
      background-size: 20px 20px; /* Grid size */
      color: var(--blueprint-text);
      font-family: 'Roboto Mono', monospace;
      font-size: 14px;
      line-height: 1.6;
      margin: 0;
      padding: 30px;
    }
    .blueprint-container {
      background-color: var(--blueprint-container-bg);
      border: 1px solid var(--blueprint-line);
      padding: 20px 30px;
      max-width: 900px;
      margin: 0 auto;
      position: relative;
      box-shadow: 0 0 15px rgba(255, 255, 255, 0.1);
    }
    .title-block {
        border: 1px solid var(--blueprint-line);
        position: absolute;
        bottom: 10px;
        right: 10px;
        padding: 5px 10px;
        font-size: 10px;
        background-color: var(--blueprint-bg);
        text-align: right;
        color: var(--blueprint-line); /* White text in title block */
    }
    .title-block div { margin-bottom: 2px; }
    .handwritten {
        font-family: 'Architects Daughter', cursive;
        color: var(--blueprint-text); /* Light cyan annotations */
        font-size: 0.9em;
    }
    /* Markdown content styling */
    h1, h2, h3 {
      color: var(--blueprint-line); /* White headings */
      border-bottom: 1px solid var(--blueprint-line);
      padding-bottom: 5px;
      margin-top: 1.5em;
      margin-bottom: 0.8em;
      font-weight: 700;
      text-transform: uppercase;
    }
    h1 { font-size: 1.8em; }
    h2 { font-size: 1.4em; }
    h3 { font-size: 1.1em; }
    p { margin: 0.8em 0; }
    ul, ol {
      padding-left: 30px;
      margin: 0.8em 0;
    }
    li {
      margin-bottom: 0.3em;
    }
    code {
      background-color: rgba(255, 255, 255, 0.1);
      color: var(--blueprint-accent);
      padding: 0.2em 0.4em;
      border-radius: 3px;
    }
    pre {
      background-color: rgba(0, 0, 0, 0.2);
      border: 1px dashed var(--blueprint-line);
      padding: 15px;
      margin: 1.5em 0;
      overflow-x: auto;
      font-size: 0.9em;
      position: relative;
      color: var(--blueprint-text);
    }
    pre::before { /* Schematic symbol decoration */
        content: '// DETAIL VIEW'; /* Changed text */
        position: absolute;
        top: -10px;
        left: 10px;
        font-family: 'Architects Daughter', cursive;
        color: var(--blueprint-line);
        font-size: 0.8em;
        background-color: var(--blueprint-bg);
        padding: 0 5px;
    }
    pre code {
      background-color: transparent;
      border: none;
      padding: 0;
      color: inherit; /* Inherit text color */
    }
    blockquote {
      border-left: 4px solid var(--blueprint-accent);
      padding: 10px 15px;
      margin: 1.5em 0;
      background-color: var(--blueprint-container-bg);
      color: var(--blueprint-text);
      font-style: italic;
      position: relative;
    }
    blockquote::before { /* Annotation symbol */
        content: '> NOTE:';
        display: block;
        font-family: 'Architects Daughter', cursive;
        font-style: normal;
        font-size: 0.9em;
        color: var(--blueprint-line);
        margin-bottom: 5px;
    }
    a {
      color: var(--blueprint-accent);
      text-decoration: none;
      border-bottom: 1px dashed var(--blueprint-accent);
    }
    a:hover {
      color: var(--blueprint-line);
      border-bottom-style: solid;
    }
    hr {
        border: none;
        border-top: 1px dashed var(--blueprint-line); /* White dashed line */
        margin: 2em 0;
    }
    table {
      border-collapse: collapse;
      margin: 1.5em 0;
      width: 100%;
      border: 1px solid var(--blueprint-line);
      font-size: 0.9em;
    }
    th, td {
      border: 1px dashed var(--blueprint-line); /* Dashed internal lines */
      padding: 0.5em 0.7em;
      text-align: left;
    }
    th {
      background-color: rgba(255, 255, 255, 0.1);
      color: var(--blueprint-line);
      font-weight: 700;
      text-transform: uppercase;
      border-bottom: 1px solid var(--blueprint-line);
    }
    td {
        color: var(--blueprint-text);
    }
  </style>
</head>
<body>
  <div class="blueprint-container">
    {{{content}}}

    <div class="title-block">
      <div><span class="handwritten">PROJECT:</span> STYLEMD</div>
      <div><span class="handwritten">SHEET:</span> 01</div>
      <div><span class="handwritten">DATE:</span> {{formatDate}}</div>
      <div><span class="handwritten">REV:</span> B</div> <!-- Revision up -->
    </div>
  </div>
</body>
</html> 