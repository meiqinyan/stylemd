<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pixel Quest Log</title>
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&amp;display=swap" rel="stylesheet"/>
  <style>
    body {
      background-color: #222034; /* Dark Purple/Blue */
      color: #ffffff;
      font-family: 'Press Start 2P', cursive;
      font-size: 10px; /* Smaller pixel font size */
      line-height: 1.6;
      margin: 0;
      padding: 15px;
      image-rendering: pixelated; /* Keep images sharp */
    }
    .pixel-window {
      background-color: #444060;
      border: 4px solid #ffffff; /* Thick white border */
      box-shadow: 0 0 0 4px #9e9e9e, 0 0 0 8px #000000; /* Gray and Black outer borders */
      padding: 10px;
      max-width: 600px;
      margin: 20px auto;
    }
    .pixel-header {
        background-color: #757575; /* Gray Header */
        color: #ffffff;
        padding: 4px 6px;
        margin: -10px -10px 10px -10px; /* Extend to edges */
        text-align: center;
        border-bottom: 4px solid #ffffff;
        font-size: 12px;
    }
    /* Markdown styling */
    h1, h2, h3 {
      color: #facc22; /* Yellow Gold */
      margin-top: 1.5em;
      margin-bottom: 0.7em;
      font-weight: normal;
      text-transform: uppercase;
    }
    h1 { font-size: 1.4em; }
    h2 { font-size: 1.2em; }
    h3 { font-size: 1em; }
    p { margin: 0.8em 0; }
    ul, ol {
        padding-left: 0;
        margin: 0.8em 0;
        list-style: none;
    }
    li::before {
        content: "* ";
        color: #facc22;
    }
    code {
      background-color: #757575;
      color: #ffffff;
      padding: 1px 3px;
      border: 1px solid #ffffff;
    }
    pre {
      background-color: #222034;
      /* Use double border for window effect */
      border: 4px solid #ffffff;
      box-shadow: 0 0 0 4px #000000;
      padding: 8px;
      margin: 1em 0;
      overflow-x: auto;
      line-height: 1.4;
    }
    pre code {
      background-color: transparent;
      border: none;
      padding: 0;
    }
    blockquote {
      border: 2px solid #facc22;
      padding: 8px;
      margin: 1em 0;
      background-color: rgba(250, 204, 34, 0.1);
      color: #f0f0f0;
    }
    blockquote p { margin: 0.3em 0; }
    hr {
        border: none;
        height: 4px;
        background-image: linear-gradient(to right, #9e9e9e 2px, transparent 2px), linear-gradient(to right, #9e9e9e 2px, transparent 2px);
        background-size: 4px 2px;
        background-position: 0 0, 0 2px;
        background-repeat: repeat-x;
        margin: 1.5em 0;
    }
    table {
        border-collapse: collapse;
        margin: 1em 0;
        border: 2px solid #ffffff;
    }
    th, td {
        border: 2px solid #9e9e9e;
        padding: 5px 8px;
        text-align: left;
    }
    th {
        background-color: #757575;
        color: #ffffff;
        font-weight: normal;
    }
    .pixel-button {
        background-color: #9e9e9e;
        color: #ffffff;
        border: 4px solid #ffffff;
        box-shadow: 0 0 0 4px #000000;
        padding: 5px 10px;
        text-decoration: none;
        display: inline-block;
        margin: 10px 5px 0 5px;
        cursor: pointer;
    }
    .pixel-button:hover {
        background-color: #bdbdbd;
    }
  </style>
</head>
<body>
  <div class="pixel-window">
    <div class="pixel-header">PLAYER STATUS</div>
    {{{content}}}
    <div style="text-align: center;">
        <a href="#" class="pixel-button">OK</a>
        <a href="#" class="pixel-button">CANCEL</a>
    </div>
  </div>
</body>
</html> 