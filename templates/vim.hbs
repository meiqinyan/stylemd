<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Vim Buffer</title>
  <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400&amp;display=swap" rel="stylesheet" type="text/css"/>
  <style type="text/css">
    /* Vim-inspired theme - Solarized Dark */
    :root {
      --vim-bg: #002b36; /* Solarized Dark Background */
      --vim-fg: #93a1a1; /* Increased brightness for main text */
      --vim-border: #073642;
      --vim-comment: #657b83; /* Increased brightness for comments */
      --vim-keyword: #859900;
      --vim-string: #2aa198;
      --vim-number: #d33682;
      --vim-function: #268bd2;
      --vim-operator: #cb4b16;
      --vim-status-bg: #073642;
      --vim-status-fg: #93a1a1;
      --vim-visual-bg: #073642; /* Background for visual mode indication */
    }
    body {
      background-color: var(--vim-bg);
      color: var(--vim-fg);
      font-family: 'Fira Code', Consolas, monospace;
      font-size: 14px;
      line-height: 1.4;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .vim-buffer {
      flex-grow: 1;
      padding: 5px 10px;
      overflow-y: auto;
    }
    .vim-statusline {
      background-color: var(--vim-status-bg);
      color: var(--vim-status-fg);
      padding: 1px 8px;
      font-size: 12px;
      display: flex;
      justify-content: space-between;
      white-space: nowrap;
      border-top: 1px solid var(--vim-border);
    }
    .vim-status-left {
      font-weight: bold;
    }
    .vim-status-right span {
      margin-left: 10px;
    }
    .vim-mode {
        background-color: var(--vim-keyword);
        color: var(--vim-bg);
        padding: 0 4px;
        margin-right: 5px;
        font-weight: bold;
    }
    /* Markdown styling */
    h1, h2, h3 {
      color: var(--vim-function);
      margin-top: 1.2em;
      margin-bottom: 0.6em;
      font-weight: bold;
      border-bottom: 1px solid var(--vim-border);
      padding-bottom: 3px;
    }
    h1 { font-size: 1.5em; }
    h2 { font-size: 1.3em; }
    h3 { font-size: 1.1em; }
    p { margin: 0.7em 0; }
    ul, ol {
      padding-left: 25px;
      margin: 0.7em 0;
    }
    li { margin-bottom: 0.3em; }
    code {
      color: var(--vim-operator);
      background-color: var(--vim-visual-bg);
      padding: 0.1em 0.3em;
      border-radius: 2px;
    }
    pre {
      background-color: var(--vim-visual-bg);
      border: 1px solid var(--vim-border);
      padding: 10px;
      margin: 1em 0;
      overflow-x: auto;
    }
    pre code {
      background-color: transparent;
      padding: 0;
      border: none;
      color: inherit; /* Inherit from parent pre */
    }
    /* Basic syntax highlighting simulation within pre */
    pre .comment { color: var(--vim-comment); font-style: italic; }
    pre .keyword { color: var(--vim-keyword); font-weight: bold; }
    pre .string { color: var(--vim-string); }
    pre .number { color: var(--vim-number); }
    pre .function { color: var(--vim-function); }
    pre .operator { color: var(--vim-operator); }
    pre .variable { color: var(--vim-fg); } /* Default text color for variables */
    pre .builtin { color: var(--vim-keyword); } /* Treat builtins like keywords */

    blockquote {
      border-left: 3px solid var(--vim-comment);
      padding-left: 10px;
      margin: 1em 0;
      color: var(--vim-comment);
      font-style: italic;
    }
    table {
        border-collapse: collapse;
        margin: 1em 0;
        width: 100%;
        border: 1px solid var(--vim-border);
    }
    th, td {
        border: 1px solid var(--vim-border);
        padding: 5px 8px;
        text-align: left;
    }
    th {
        background-color: var(--vim-status-bg);
        font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="vim-buffer">
    {{{content}}}
  </div>
  <div class="vim-statusline">
    <div class="vim-status-left">
      <span class="vim-mode">-- NORMAL --</span>
      <span>example.md</span>
    </div>
    <div class="vim-status-right">
      <span>Ln 1, Col 1</span>
      <span>100%</span>
      <span>unix [utf-8]</span>
    </div>
  </div>
</body>
</html> 