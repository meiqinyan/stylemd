<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="generator" content="stylemd">
  <title>Commodore 64 Screen</title>
  <link href="https://fonts.googleapis.com/css2?family=Cutive+Mono&family=Press+Start+2P&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #352879; /* C64 Dark Blue */
      color: #6c5eb5; /* C64 Light Blue */
      font-family: 'Press Start 2P', 'Cutive Mono', monospace; /* Pixel font first, fallback */
      font-size: 14px; /* Adjust for readability */
      line-height: 1.4;
      padding: 20px;
      margin: 0;
      /* Simulate screen border */
      border: 20px solid #6c5eb5; /* Light blue border */
      min-height: calc(100vh - 40px); /* Adjust for border */
      box-sizing: border-box;
    }
    .c64-output {
      white-space: pre-wrap; /* Allow wrapping */
      word-break: break-all; /* Break long words if needed */
    }
    .c64-ready {
      color: #9a8ffb; /* Lighter purple for READY */
    }
    .c64-cursor {
      background-color: #6c5eb5; /* Light Blue cursor block */
      animation: blink 1s step-end infinite;
      display: inline-block;
      width: 0.6em; /* Adjust width based on font */
      height: 1.2em; /* Adjust height based on font */
      vertical-align: text-bottom;
    }
    @keyframes blink {
      50% { opacity: 0; }
    }
    /* Markdown styling */
    h1, h2, h3 {
      color: #ffffff; /* White headings */
      margin-top: 1em;
      margin-bottom: 0.5em;
      font-weight: normal;
      text-transform: uppercase;
    }
    h1 { font-size: 1.2em; }
    h2 { font-size: 1.1em; }
    h3 { font-size: 1em; }
    p { margin: 0.5em 0; }
    ul, ol {
        padding-left: 0;
        margin: 0.5em 0;
        list-style: none;
    }
    li::before {
        content: "* ";
        color: #ffffff; /* White bullet */
    }
    code {
      color: #95e064; /* C64 Light Green */
      background-color: transparent;
    }
    pre {
      /* Simulate BASIC listing */
      background-color: #352879; /* Same as body */
      color: #ffffff; /* White code */
      border: none;
      padding: 0;
      margin: 1em 0;
      overflow-x: auto;
      font-family: 'Cutive Mono', 'Press Start 2P', monospace; /* Use smoother mono for code */
      line-height: 1.3;
      white-space: pre; /* Important: Keep original whitespace */
      counter-reset: line;
    }
    pre code {
      color: #ffffff;
      display: block; /* Make code block display: block */
    }
    /* Remove old line number style */
    /* pre code::before { ... } */

    /* Apply line numbers to each .code-line span */
    .code-line {
        display: block; /* Each line is a block */
    }
    .code-line::before {
        counter-increment: line;
        content: counter(line, decimal);
        display: inline-block;
        width: 3em; /* Space for line numbers */
        padding-right: 1em;
        margin-right: 1em;
        text-align: right;
        color: #7e75c4; /* Slightly brighter light blue */
        /* Prevent selection if needed */
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
    }
    blockquote {
        border: 1px solid #6c5eb5;
        padding: 5px 10px;
        margin: 1em 0;
        color: #b8b8b8; /* Grayish for quotes */
    }
    blockquote p { margin: 0.3em 0; }
    table {
        border-collapse: collapse;
        margin: 1em 0;
        border: 1px solid #6c5eb5;
        width: 100%;
    }
    th, td {
        border: 1px solid #6c5eb5;
        padding: 4px 6px;
        text-align: left;
    }
    th {
        background-color: #6c5eb5;
        color: #352879; /* Dark blue header text */
        font-weight: normal;
    }
  </style>
</head>
<body>
  <div class="c64-output">
    **** COMMODORE 64 BASIC V2 ****

    64K RAM SYSTEM  38911 BASIC BYTES FREE

    <span class="c64-ready">READY.</span>
    LOAD "NOTES",8,1

    SEARCHING FOR NOTES
    LOADING
    <span class="c64-ready">READY.</span>
    LIST

    {{{content}}}

    <span class="c64-ready">READY.</span>
    <span class="c64-cursor"></span>
  </div>
</body>
</html> 